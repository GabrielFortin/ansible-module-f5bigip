- name: Create GTM Monitor Firepass
  f5bigip_gtm_monitor_firepass:
    f5_hostname: "{{ bigip_host }}"
    f5_username: "{{ bigip_username }}"
    f5_password: "{{ bigip_password }}"
    f5_port: "{{ bigip_port }}"
    name: "{{ monitor_firepass_name }}"
    partition: "{{ bigip_partition }}"
    description: "{{ monitor_firepass_description }}"
    state: present
  delegate_to: localhost
  register: result

- name: Assert Create GTM Monitor Firepass
  assert:
    that:
      - result|changed

- name: Create GTM Monitor Firepass (idempotent)
  f5bigip_gtm_monitor_firepass:
    f5_hostname: "{{ bigip_host }}"
    f5_username: "{{ bigip_username }}"
    f5_password: "{{ bigip_password }}"
    f5_port: "{{ bigip_port }}"
    name: "{{ monitor_firepass_name }}"
    partition: "{{ bigip_partition }}"
    description: "{{ monitor_firepass_description }}"
    state: present
  delegate_to: localhost
  register: result

- name: Assert Create GTM Monitor Firepass (idempotent)
  assert:
    that:
      - not result|changed

- name: Change GTM Monitor Firepass description
  f5bigip_gtm_monitor_firepass:
    f5_hostname: "{{ bigip_host }}"
    f5_username: "{{ bigip_username }}"
    f5_password: "{{ bigip_password }}"
    f5_port: "{{ bigip_port }}"
    name: "{{ monitor_firepass_name }}"
    partition: "{{ bigip_partition }}"
    description: "new {{ monitor_firepass_description }}"
    state: present
  delegate_to: localhost
  register: result

- name: Assert Change GTM Monitor Firepass description
  assert:
    that:
      - result|changed

- name: Change GTM Monitor Firepass description (idempotent)
  f5bigip_gtm_monitor_firepass:
    f5_hostname: "{{ bigip_host }}"
    f5_username: "{{ bigip_username }}"
    f5_password: "{{ bigip_password }}"
    f5_port: "{{ bigip_port }}"
    name: "{{ monitor_firepass_name }}"
    partition: "{{ bigip_partition }}"
    description: "new {{ monitor_firepass_description }}"
    state: present
  delegate_to: localhost
  register: result

- name: Assert Change GTM Monitor Firepass description (idempotent)
  assert:
    that:
      - not result|changed

- name: Delete GTM Monitor Firepass
  f5bigip_gtm_monitor_firepass:
    f5_hostname: "{{ bigip_host }}"
    f5_username: "{{ bigip_username }}"
    f5_password: "{{ bigip_password }}"
    f5_port: "{{ bigip_port }}"
    name: "{{ monitor_firepass_name }}"
    partition: "{{ bigip_partition }}"
    state: absent
  delegate_to: localhost
  register: result

- name: Assert Delete GTM Monitor Firepass
  assert:
    that:
      - result|changed

- name: Delete GTM Monitor Firepass (idempotent)
  f5bigip_gtm_monitor_firepass:
    f5_hostname: "{{ bigip_host }}"
    f5_username: "{{ bigip_username }}"
    f5_password: "{{ bigip_password }}"
    f5_port: "{{ bigip_port }}"
    name: "{{ monitor_firepass_name }}"
    partition: "{{ bigip_partition }}"
    state: absent
  delegate_to: localhost
  register: result

- name: Assert Delete GTM Monitor Firepass (idempotent)
  assert:
    that:
      - not result|changed